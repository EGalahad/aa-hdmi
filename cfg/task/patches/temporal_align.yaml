# @package task

reward:
  tracking:
    tracking_root_pos_aligned:
      enabled: true
    tracking_root_pos:
      enabled: false

termination:
  root_pos_error_aligned:
    enabled: true
  root_pos_error:
    enabled: false
